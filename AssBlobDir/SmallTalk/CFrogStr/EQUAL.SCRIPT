= other | idx=CFrogNum sz=CFrogNum |
  other size ~= self size ifTrue: [ ^ 0 ] .
  idx _ 0 .
  sz _ self size .
  [ 
    idx _ idx + 1 .
    idx - 1 < sz
  ] whileTrue: [
    (self at: idx - 1) ~= (other at: idx - 1) ifTrue: [
     ^ 0
    ]
  ] .
  ^ 1 .